# Put your actual OAuth & SMTP credentials here or set environment variables.
# Restart your Spring Boot backend after filling this in!
spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: "${GOOGLE_CLIENT_ID:YOUR_GOOGLE_CLIENT_ID}"
            client-secret: "${GOOGLE_CLIENT_SECRET:YOUR_GOOGLE_CLIENT_SECRET}"
  mail:
    username: "${SMTP_USERNAME:codearena7.0@gmail.com}"
    password: "${SMTP_PASSWORD:YOUR_SMTP_APP_PASSWORD}"

app:
  jwt:
    secret: "${JWT_SECRET:9a8b7c6d5e4f3a2b1c0d9e8f7a6b5c4d3e2f1a0b9c8d7e6f5a4b3c2d1e0f9a8b}"
